# Fraud-shield
FraudShield AI is an interactive web platform designed to protect users from web threats before they click. It extracts 16 structural and lexical domain features, leverages a Random Forest classifier to evaluate risk levels, and uses SHAP values to explain prediction metrics transparently—complete with automated PDF security audit reports.
